<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qf.nauth.modules.auth.dao.TokenDao">
<!-- 
	<select id="get" parameterType="String" resultType="QaToken">
		SELECT token, cre_time creTime, cid 
		FROM qa_token
		WHERE cid = #{cid}
	</select>
	
	<select id="getCreTime" parameterType="String" resultType="java.util.Date">
		SELECT cre_time 
		FROM qa_token
		WHERE token = #{token}
	</select>
	
	<insert id="save" parameterType="java.util.HashMap">
		INSERT INTO qa_token VALUES (#{token}, #{creTime}, #{cid})
	</insert>
	
	<update id="update" parameterType="java.util.HashMap">
		UPDATE qa_token set token = #{token}, cre_time = #{creTime}
		WHERE cid = #{cid}
	</update>
     -->
</mapper>